未完成:
	主页面:
		缺个后端的热力图api
		缺个美工的图标 (百度地图上的商铺的地标)
		经纬度转换 (没什么必要做, 效果不明显)
		点击地点响应商铺列表 (找不到对应的百度地图api, 应该是做不了)
		响应式布局 (大工程, 得改一大半代码, 但作用不大, 只是老师喜欢这样搞, 正在磨)
	商铺页面:
		缺个后端的api和api说明书
	追lwy

技巧:
	url => xxx?mode=mock 使用假数据, 不需要后端api